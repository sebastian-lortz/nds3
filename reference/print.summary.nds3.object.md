# Print summary for summary.nds3.object

A minimal S3 print method for objects of class \`summary.nds3.object\`,
providing optimization diagnostics and summary statistics in the
console.

## Usage

``` r
# S3 method for class 'summary.nds3.object'
print(x, ...)
```

## Arguments

- x:

  A \`summary.nds3.object\` produced by \`summary.nds3.object\`.

- ...:

  Unused. Additional arguments are ignored.

## Value

Invisibly returns the input object \`x\` after printing its summary.
